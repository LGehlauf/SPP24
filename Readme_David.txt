main: meine aktuelle version (ich weiß, der name ist nicht so günstig, aber so nennt pycharm mein .py)
daten: csv-datei. da ist mein ziel, dass man mit numpy unsere einzelnen bearbeitungszeiten aus
den arbeitsplänen kopiert. so kann jeder schritt genau simuliert werden
(die grundlegende implementierung klappt schon)
überlegung: alle teile in eine csv (array-muss koordiniert werden)
oder für jedes teil eine eigene csv